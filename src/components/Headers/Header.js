import React from 'react';
import './Header.css';

const Header = props => (
    <header className="headerText">
        <h3 className="headerTextTitle">React Firebase Note APP</h3>
    </header>
);

export default Header;
